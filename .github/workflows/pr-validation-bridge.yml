name: One-time PR validation bridge

on:
  schedule:
    - cron: '10 12 25 8 *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    uses: octoleo/octoscribe/.github/workflows/ci.yml@feature/verbatim-ensemble-transcription
